### File managed by puppet
### Directory-based for the main server

<Directory "<%= scope.lookupvar('webman::install::wmdirectory') -%>">

    AllowOverride None
    <IfVersion >= 2.4>
        Require all granted
    </IfVersion>
    <IfVersion < 2.4>
        Order allow,deny
        Allow from all
    </IfVersion>

</Directory>

Alias /manpage <%= scope.lookupvar('webman::install::wmdirectory') -%>
